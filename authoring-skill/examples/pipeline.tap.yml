version: tapstate/v1
kind: pipeline
id: e2e_pipeline
source: src_file
settings: { read_mode: snapshot_and_cdc }
transforms:
  - { id: even_orders, from: [orders], type: filter, expr: "int(after.id) % 2 == 0" }
serve:
  from: even_orders
  sync:
    - source: tgt_file
