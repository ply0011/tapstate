version: tapstate/v1
kind: pipeline
id: big_orders
source: orders_src
settings: { read_mode: snapshot_and_cdc }
transforms:
  - { id: big, from: [orders], type: filter, expr: "double(after.amount) > 100.0" }
serve:
  from: big
  sync:
    - source: orders_tgt
      write_mode: upsert
