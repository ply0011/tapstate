version: tapstate/v1
kind: pipeline
id: big_orders
source: orders_src
transforms:
  - id: big
    type: filter
    from: [orders]
    expr: "double(after.amount) > 100.0"
serve:
  id: serve
  from: big
  sync:
    - id: sync_1
      source: orders_tgt
