version: tapstate/v1
kind: pipeline
id: shaped
source: orders_src
transforms:
  - id: shape
    type: map
    from: [orders]
    fields:
      order_id: after.id
      total: double(after.price) * double(after.qty)
serve:
  id: serve
  from: shape
  sync:
    - id: sync_1
      source: orders_tgt
