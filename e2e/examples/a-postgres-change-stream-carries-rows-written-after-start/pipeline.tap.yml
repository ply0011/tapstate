version: tapstate/v1
kind: pipeline
id: postgres2mongo
source: src_postgres
settings: { read_mode: snapshot_and_cdc }
transforms:
  - { id: rows_through, from: [orders], type: filter, expr: "op == 'r' || op == 'i'" }
serve:
  from: rows_through
  sync:
    - source: tgt_mongo
